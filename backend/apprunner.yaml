version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements-apprunner.txt
run:
  command: gunicorn --bind 0.0.0.0:$PORT token_server:app
  network:
    port: 8080
    env: PORT

