version: 1.0
runtime: python311
build:
  commands:
    build:
      - true
run:
  pre-run:
    - pip3 install -r requirements.txt
  command: gunicorn --bind 0.0.0.0:$PORT token_server:app
  network:
    port: 8080
    env: PORT