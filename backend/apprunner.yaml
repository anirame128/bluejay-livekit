version: 1.0
runtime: python311
build:
  commands:
    build:
      - true
run:
  pre-run:
    - pip3 install -r requirements.txt
  command: python3 application.py
  network:
    port: 8080
  env:
    - name: PORT
      value: "8080"